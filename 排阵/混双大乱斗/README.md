# 混双大乱斗排阵系统

## 功能说明

A组男队员和B组女队员依次组合对战，支持2个场地同时进行比赛。

### 核心特性

✅ **并发进行**：2个场地同时比赛，提高比赛效率  
✅ **球员不重复**：同一轮次中，每个球员只能出现在一个场地  
✅ **合理配对**：确保每场比赛的4名球员都不同（无相同男队员或女队员）  
✅ **场次均衡**：自动分配，让每位球员的参赛次数相对均匀  
✅ **优先排阵**：支持指定球员优先排阵（如需要提前回家的球员）  
✅ **Excel导出**：生成格式规范的对阵表Excel文件

## 使用方法

### 1. 修改球员名单

编辑 `mixed_doubles_chaos.py` 文件中的 `main()` 函数，修改球员名单：

```python
group_a_males = ["林锋", "王小波", "陈顺星", "罗琴荩", "苏大哲", "黄冬青"]
group_b_females = ["田茜", "李祺祺", "高洁", "滕菲", "崔倩男", "李杏芝"]
```

### 2. 运行测试（推荐）

在生成对阵表之前，建议先运行测试验证功能正常：

```bash
cd /Users/yanyongwen712/Documents/pingan_tech_badminton_team/排阵/混双大乱斗
python run_tests.py
```

测试会验证：
- ✅ 优先排阵球员在前6轮连续上场
- ✅ 所有球员场次均衡（每人6场）
- ✅ 同一轮次球员不重复
- ✅ 比赛配对有效（无相同男队员或女队员）

### 3. 运行脚本

```bash
python mixed_doubles_chaos.py
```

### 3. 查看结果

- 控制台会输出详细的赛程摘要
- 自动生成 `混双大乱斗_对阵表.xlsx` 文件

## 示例输出

### A组男队员（6人）
林锋, 王小波, 陈顺星, 罗琴荩, 苏大哲, 黄冬青

### B组女队员（6人）
田茜, 李祺祺, 高洁, 滕菲, 崔倩男, 李杏芝

### 生成结果
- **总轮数**：15轮
- **总场次**：30场
- **每轮场地**：2个场地同时进行
- **每轮人数**：8人（4对混双）

#### 第1轮示例
- **1号场地**：王小波/高洁 VS 黄冬青/滕菲
- **2号场地**：陈顺星/田茜 VS 罗琴荩/李杏芝

## 高级配置

### 调整最大轮数

在调用 `generate_mixed_doubles_matches()` 时，可以指定 `max_rounds` 参数：

```python
matches = generate_mixed_doubles_matches(
    group_a_males=group_a_males,
    group_b_females=group_b_females,
    court_count=2,
    max_rounds=20  # 自定义最大轮数
)
```

如果不指定，默认会根据人数自动计算（最多15轮）。

### 调整场地数量

```python
matches = generate_mixed_doubles_matches(
    group_a_males=group_a_males,
    group_b_females=group_b_females,
    court_count=3  # 3个场地同时进行
)
```

### 优先排阵（指定球员提前上场）

如果有球员需要提前回家或有其他特殊情况，可以使用 `priority_players` 参数让这些球员优先排阵，在前面的轮次连续上场而不休息：

```python
matches = generate_mixed_doubles_matches(
    group_a_males=group_a_males,
    group_b_females=group_b_females,
    court_count=2,
    mode="fair",
    priority_players=["崔倩男"]  # 崔倩男优先排阵，前6轮连续上场
)
```

也可以指定多个优先球员：

```python
priority_players=["崔倩男", "李杏芝"]  # 两人都优先排阵
```

## 验证规则

系统会自动验证以下规则：

1. ✅ 同一轮次中，每个球员只能出现在一个场地
2. ✅ 每场比赛中，两个配对的男队员不能相同
3. ✅ 每场比赛中，两个配对的女队员不能相同
4. ✅ 不会重复使用相同的对决组合

详细的测试说明请参考 [测试说明.md](./测试说明.md)

## 测试套件

项目提供了完整的测试套件，用于验证优先排阵功能的正确性。

### 快速测试
```bash
python run_tests.py
```

### 完整测试
```bash
python test_priority_comprehensive.py
```

### 简单对比测试
```bash
python test_priority.py
```

所有测试都基于实际代码运行结果进行验证，确保必须需求100%通过。

## Excel格式

生成的Excel文件包含以下列：

| 列名 | 说明 |
|------|------|
| 轮次 | 第几轮比赛 |
| 场地 | 几号场地 |
| 类型 | 比赛类型（混双） |
| 对阵 A (男/女) | A队配对（男队员/女队员） |
| 比分 A | A队比分（留空待填） |
| 比分 B | B队比分（留空待填） |
| 对阵 B (男/女) | B队配对（男队员/女队员） |

## 注意事项

1. **人数要求**：至少需要2名男队员和2名女队员才能生成比赛
2. **随机性**：每次运行结果可能不同（使用了随机打乱）
3. **场次分布**：由于是随机安排，球员的参赛场次会有差异（通常在合理范围内）

## 技术实现

- **语言**：Python 3
- **依赖库**：openpyxl（用于Excel导出）
- **算法**：组合枚举 + 约束满足

## 常见问题

### Q: 为什么有些球员场次多，有些少？
A: 由于随机性和约束条件，场次分布会有一定差异。如果需要更均衡的分配，可以增加轮数或调整算法。

### Q: 可以生成更多轮次吗？
A: 可以，通过设置 `max_rounds` 参数来增加轮数。但要注意，随着轮数增加，找到符合条件的组合会越来越难。

### Q: 支持其他比赛类型吗？
A: 当前版本仅支持混双对决。如需支持其他类型（如男双、女双），可以参考项目中的其他排阵脚本。
